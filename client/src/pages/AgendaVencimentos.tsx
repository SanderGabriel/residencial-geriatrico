import { Placeholder } from './Placeholder';

export function AgendaVencimentosPage() {
  return (
    <Placeholder
      titulo="Agenda de Vencimentos"
      descricao="Timeline visual de títulos próximos a vencer"
      proxima="Requer biblioteca de calendário (ex: react-day-picker) e cálculo de status vencido/próximo. Mover para Phase 2 — os títulos já estão acessíveis via /titulos-pagar e /titulos-receber."
    />
  );
}
